# Keep real values only in .env.local (which is gitignored).
ABUSEIPDB_API_KEY=
VIRUSTOTAL_API_KEY=
ETHERSCAN_API_KEY=
ALIENVAULT_OTX_KEY=
WHOISXML_API_KEY=
GOOGLE_SAFE_BROWSING_KEY=

# Best-effort per-instance API limit. Use a shared store such as Upstash Redis
# when deploying across multiple serverless instances.
RATE_LIMIT_REQUESTS_PER_MINUTE=30
